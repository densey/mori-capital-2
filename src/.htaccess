# Deny direct web access to the /src/ folder.
# OpenLiteSpeed / Apache / Nginx (where applicable).

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
